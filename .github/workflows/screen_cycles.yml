name: Screener de cycles

# À la demande : tu colles une liste d'entreprises, il te dit lesquelles ont un
# cycle intéressant (⭐⭐ / ⭐ / ➖ / ✗). Résultat dans le log ET sur Telegram.
on:
  workflow_dispatch:
    inputs:
      tickers:
        description: "Symboles Yahoo séparés par des espaces ou virgules (ex: XLE XME GDX EWZ KRE)"
        required: true
        type: string
      period:
        description: "Fenêtre glissante (ignorée si 'start' est renseigné)"
        required: false
        default: "10y"
        type: string
      start:
        description: "Date de début FIXE (JJ/MM/AAAA) → stats figées. Vide = fenêtre glissante"
        required: false
        default: ""
        type: string
      min_zones:
        description: "Zones minimum pour qu'un cycle soit jugé fiable"
        required: false
        default: "10"
        type: string
      min_hit:
        description: "% de réussite minimum pour qu'un cycle soit jugé fiable"
        required: false
        default: "75"
        type: string
      telegram:
        description: "Envoyer aussi le résultat sur Telegram ?"
        required: false
        default: "non"
        type: choice
        options: ["non", "oui"]

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Installer les dépendances
        run: pip install numpy pandas yfinance scipy requests

      - name: Screener les cycles
        env:
          TICKERS: ${{ inputs.tickers }}
          SCREEN_PERIOD: ${{ inputs.period }}
          SCREEN_START: ${{ inputs.start }}
          MIN_ZONES: ${{ inputs.min_zones }}
          MIN_HIT: ${{ inputs.min_hit }}
          # Telegram uniquement si demandé (par défaut « non » → résultat dans le log seulement)
          TELEGRAM_TOKEN: ${{ inputs.telegram == 'oui' && secrets.TELEGRAM_TOKEN || '' }}
          TELEGRAM_CHAT_ID: ${{ inputs.telegram == 'oui' && secrets.TELEGRAM_CHAT_ID || '' }}
        run: python screen_cycles.py
